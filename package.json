{
  "name": "hello_world",
  "version": "1.0.0",
  "description": "hello world sample for NodeJS",
  "main": "app.js",
  "repository": "https://github.com/wlckconsulting/sam-template",
  "author": "SAM CLI",
  "license": "MIT",
  "dependencies": {
    "axios": ">=1.3.4",
    "uuid": "^9.0.0"
  },
  "scripts": {
    "build": "tsc && sam build",
    "deploy": "npm run build && sam deploy --guided",
    "watch": "tsc --watch",
    "start:local": "npm run watch & rm -r ../.aws-sam & sam local start-api"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.114",
    "@types/node": "^18.15.3",
    "@types/uuid": "^9.0.1",
    "aws-sdk": "^2.1338.0",
    "typescript": "^5.0.2"
  }
}
